
#include "Menu.h"

int main() {
	Menu menu;

	while (menu.start()) {													// Цикл оснновного меню


		while (menu.chatRun && menu.chat()) {								// Если выполнен вход и вызов меню чат				

		}
	}

	return 0;
}